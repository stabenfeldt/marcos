default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  user: martins
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: 5

development:
  <<: *default
  database: marcos_development

test:
  <<: *default
  database: marcos_test

production:
  <<: *default
  database: marcos_production
